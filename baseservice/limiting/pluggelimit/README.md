```
使用uber开发的go.uber.org/ratelimit@v0.2.0做二次开发，增加了超时控制
单机版
真正意义上的漏桶【堵塞式限流器】
大部分场景使用不到，因为完全不允许突发流量，恒定放出流量
对DB友好，无并发，可用来规范流量，用来做兜底防止压垮DB
把每个请求排队，每隔1s/rate放出一个请求
Take()接口会堵塞，直到请求允许放行，返回允许的时间
缺陷在于，瞬时流量过大会存在请求饿死，长时间等待【已优化】
```